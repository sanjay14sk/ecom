# Server Configuration
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/ecommerce_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=sanjay20
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Configurations
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Security JWT Configurations
# Secret key (Base64-encoded representation of a strong secret, at least 256 bits)
app.jwt.secret=======================AntigravityEcommerceSecretKeyForHS256SignatureMustBeAtLeast256BitsLong======================
app.jwt.expirationMs=86400000

# File Upload Configuration
app.upload.dir=uploads/
# Max file size configuration
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB
